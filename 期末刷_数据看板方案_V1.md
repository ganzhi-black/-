# 期末刷 — 内测数据看板方案

> 目标：搭建一套完整的数据看板，追踪内测用户从进入产品到形成训练闭环的全链路行为，定位每一步的流失节点，为产品迭代提供数据依据。

---

## 一、核心漏斗：用户全链路转化

基于产品的 7 个核心页面，用户从接触到留存的完整路径可以拆成 **6 步转化漏斗**：

```
访问首页 → 注册/登录 → 上传资料(建科目) → 设置出题 → 完成答题 → 查看错题本/重做
```

每一步都是一个潜在流失点。看板的第一优先级就是把这条链路的转化率可视化。

### 漏斗定义

| 步骤 | 事件名 | 触发条件 | 对应页面 |
|------|--------|---------|---------|
| Step 1 | `page_view_home` | 用户打开首页 | 首页 |
| Step 2 | `signup_complete` / `login_complete` | 注册成功或登录成功 | 首页 |
| Step 3 | `subject_created` | 资料上传完成，科目建库成功（status=ready） | 上传页 |
| Step 4 | `practice_started` | 点击「开始练习」，AI 出题请求发出 | 练习设置页 |
| Step 5 | `practice_completed` | 完成本轮所有题目，进入总结页 | 本轮总结页 |
| Step 6 | `retry_started` | 从错题本发起重做 | 错题本页 |

### 看板展示

**漏斗图（主视图）**：横向或纵向漏斗，每步显示人数和转化率。

**关键指标卡片**（漏斗图上方）：

| 指标 | 计算方式 | 刷新频率 |
|------|---------|---------|
| 总访问 UV | 当日/当周去重访问用户数 | 实时 |
| 注册转化率 | signup_complete / page_view_home | 每日 |
| 建库转化率 | subject_created / login_complete | 每日 |
| 完练率 | practice_completed / practice_started | 每日 |
| 错题重做率 | retry_started / practice_completed（限有错题的用户） | 每日 |

---

## 二、流失分析：每一步为什么掉人

漏斗只告诉你「哪里掉了人」，但不告诉你「为什么」。针对每个流失节点，需要埋点追踪异常行为。

### Step 1→2 流失：访问了但没注册

**可能原因 & 对应埋点**：

| 假设 | 埋点事件 | 字段 |
|------|---------|------|
| 首页没看懂产品价值 | `page_view_duration` | duration_seconds, scroll_depth_percent |
| 注册流程太长/报错 | `signup_attempt` | method(phone/wechat/qq), result(success/fail), error_type |
| 只是路过，没有需求 | `traffic_source` | source(直接/分享链接/社群/朋友圈) |

**看板展示**：注册漏斗明细（尝试次数 vs 成功次数），按来源渠道分组的转化率对比。

### Step 2→3 流失：登录了但没建科目

**可能原因 & 对应埋点**：

| 假设 | 埋点事件 | 字段 |
|------|---------|------|
| 不知道上传什么 | `page_view_upload` | stay_duration, is_bounce(进入后未操作即离开) |
| 手里没有电子版资料 | `upload_attempt` | result(success/fail), error_type(format/size/scan_pdf) |
| 上传了但建库失败 | `subject_status_change` | from_status, to_status, duration_seconds |

**看板展示**：上传成功率，失败原因分布饼图，建库耗时分布（标注 P50/P95）。

### Step 3→4 流失：建了科目但没开始练

**可能原因 & 对应埋点**：

| 假设 | 埋点事件 | 字段 |
|------|---------|------|
| 设置页选项太多犹豫 | `settings_page_interaction` | time_on_page, changed_defaults(bool) |
| 不确定选什么题型 | `settings_selection` | question_types[], count, mode |
| 建完就走，改天再来 | `session_end` | last_page, time_since_subject_created |

**看板展示**：建科目到首次练习的时间差分布，设置页停留时长 vs 是否开始练习。

### Step 4→5 流失：开始答题但没做完

**这是最关键的流失节点——直接反映产品核心体验。**

| 假设 | 埋点事件 | 字段 |
|------|---------|------|
| AI 出题太慢 | `question_generated` | generation_duration_seconds, question_count |
| 题目质量差(超纲/太简单) | `question_feedback` | question_id, feedback_type(too_hard/too_easy/irrelevant/good) |
| 语音识别体验差 | `voice_input` | duration_seconds, edit_after_asr(bool), edit_ratio |
| 做到一半放弃 | `practice_abandoned` | completed_count, total_count, last_question_type, abandon_page |
| 单题批改结果不可信 | `result_interaction` | viewed_source(bool), clicked_retry(bool), time_on_result |

**看板展示**：
- 完练率按题量分组（5题/10题/20题/30题各自的完成率）
- 中途放弃的题目序号分布（是第几题放弃的？）
- 语音输入后的编辑率（编辑率高 = ASR 体验差）
- AI 出题/批改耗时的 P50/P95

### Step 5→6 流失：做完了但不回来重做错题

| 假设 | 埋点事件 | 字段 |
|------|---------|------|
| 没注意到错题本入口 | `summary_page_interaction` | clicked_wrong_book(bool), clicked_continue(bool), clicked_home(bool) |
| 觉得练一轮够了 | `return_visit` | days_since_last_practice, triggered_by(自然回访/推送) |
| 错题本为空没错题 | `wrong_book_view` | wrong_count, is_empty |

**看板展示**：总结页三个按钮的点击分布，错题本非空用户的重做率。

---

## 三、核心体验指标（非漏斗类）

除了漏斗转化，还需要追踪产品核心体验是否成立。

### 3.1 AI 出题质量

| 指标 | 计算方式 | 健康阈值（建议） |
|------|---------|----------------|
| 出题耗时 | question_generated.duration 的 P50 | < 10 秒 |
| 客观题首次正确率 | 全部客观题 is_correct=true 的比例 | 40%-70%（太高=太简单，太低=超纲） |
| 主观题平均准确率 | 全部主观题 accuracy 的均值 | 30%-60% |
| 负反馈率 | feedback_type 为 irrelevant 的比例 | < 10% |

### 3.2 AI 批改可信度

| 指标 | 计算方式 | 意义 |
|------|---------|------|
| 原文出处查看率 | result_interaction.viewed_source=true 的比例 | 用户是否信任批改结果（不查看=信任 or 不在乎） |
| 「再说一遍」点击率 | 主观题结果页 clicked_retry=true 的比例 | 用户是否认可评分 |
| 重做后进步率 | retry 答案的 accuracy > 首次 accuracy 的比例 | 产品训练闭环是否生效 |

### 3.3 语音输入体验

| 指标 | 计算方式 | 健康阈值 |
|------|---------|---------|
| ASR 编辑率 | edit_after_asr=true 的比例 | < 30% |
| 平均编辑幅度 | 编辑后文本与 ASR 原文的编辑距离 / 原文长度 | < 20% |
| 录音重录率 | 同一题多次录音的比例 | < 15% |

---

## 四、用户分群与留存

### 4.1 用户活跃分层

| 分层 | 定义 | 看板展示 |
|------|------|---------|
| 新用户 | 注册 ≤ 3 天 | 每日新增趋势线 |
| 激活用户 | 注册后完成过至少 1 轮练习 | 激活率 = 激活用户 / 新用户 |
| 活跃用户 | 最近 3 天内有答题行为 | 活跃用户数趋势 |
| 回流用户 | 沉默 ≥ 3 天后重新答题 | 回流率 |
| 流失用户 | 注册后 ≥ 7 天无任何操作 | 流失用户数及占比 |

### 4.2 留存表

按注册日期分组（cohort），追踪 Day 1 / Day 3 / Day 7 留存率。

内测阶段重点关注：

- **D1 留存**：注册次日是否回来练习（目标 > 30%）
- **D3 留存**：是否形成短期习惯（目标 > 15%）
- **D7 留存**：是否度过新鲜期（目标 > 10%）

### 4.3 按科目类型分群

追踪不同学科类型用户的行为差异（在建科目时让用户选择或自动归类）：

- 文科 vs 商科 vs 医学 vs 法学 vs 工科
- 对比各科的完练率、主观题准确率、错题重做率

---

## 五、埋点清单汇总

以下是需要前后端配合埋入的完整事件列表，交给 Codex 实现时按此表执行：

### 页面浏览类（前端埋点）

| 事件名 | 触发时机 | 必传字段 |
|--------|---------|---------|
| `page_view` | 每次页面加载 | page_name, user_id(可空), timestamp, session_id, referrer |
| `page_leave` | 页面卸载/切换 | page_name, duration_seconds, scroll_depth_percent |

### 用户行为类（前端埋点）

| 事件名 | 触发时机 | 必传字段 |
|--------|---------|---------|
| `signup_attempt` | 点击注册按钮 | method, timestamp |
| `signup_complete` | 注册成功回调 | method, user_id, timestamp |
| `login_complete` | 登录成功回调 | method, user_id, timestamp |
| `upload_attempt` | 点击上传/拖入文件 | file_type, file_size_kb, timestamp |
| `upload_result` | 上传接口返回 | result(success/fail), error_type, timestamp |
| `settings_selection` | 点击「开始练习」 | question_types[], count, mode, subject_id |
| `voice_input` | 语音录制完成 | duration_seconds, asr_text_length, question_id |
| `voice_edit` | 用户编辑 ASR 文本后提交 | original_length, edited_length, edit_distance |
| `question_feedback` | 用户对题目打标（可选功能） | question_id, feedback_type |
| `result_interaction` | 单题结果页操作 | question_id, viewed_source, clicked_retry, time_on_result_seconds |
| `summary_interaction` | 总结页按钮点击 | clicked_button(continue/wrong_book/home) |
| `retry_started` | 错题本发起重做 | question_ids[], retry_count |

### 业务数据类（后端埋点/数据库直接查询）

| 事件名/数据源 | 说明 | 必传字段 |
|-------------|------|---------|
| `subject_created` | 科目建库完成 | subject_id, user_id, chunk_count, build_duration_seconds |
| `subject_failed` | 科目建库失败 | subject_id, user_id, error_type |
| `question_generated` | AI 出题完成 | subject_id, question_count, generation_duration_seconds, question_types[] |
| `answer_submitted` | 每道题提交答案 | question_id, question_type, is_correct, accuracy, is_retry |
| `practice_completed` | 本轮练习完成 | session_id, total_count, correct_count, avg_accuracy, duration_seconds |
| `practice_abandoned` | 用户中途退出答题 | session_id, completed_count, total_count, last_question_type |

---

## 六、技术实现建议

### 6.1 埋点技术栈（推荐给 Codex）

内测阶段不需要重型数据平台，优先轻量可用：

**方案 A — 最轻量（推荐内测前 100 用户）**：
- 前端：自定义 `track()` 函数，将事件 POST 到业务后端的 `/api/events` 接口
- 后端：事件写入一张 `events` 表（event_name, user_id, properties JSON, timestamp）
- 看板：用 Metabase（开源）连接同一个数据库，配置 SQL 查询 + 可视化

**方案 B — 中等复杂度（100-1000 用户）**：
- 前端：接入 Mixpanel / GrowingIO / 神策 的 JS SDK（免费额度通常够内测用）
- 看板：直接用平台自带的漏斗、留存分析功能

### 6.2 events 表设计（方案 A）

```sql
CREATE TABLE events (
    id          BIGINT AUTO_INCREMENT PRIMARY KEY,
    event_name  VARCHAR(50)  NOT NULL,
    user_id     BIGINT       NULL,         -- 未登录时为空
    session_id  VARCHAR(64)  NOT NULL,     -- 前端生成的会话 ID
    properties  JSON         NOT NULL,     -- 事件自定义字段
    page_name   VARCHAR(30)  NULL,
    timestamp   DATETIME(3)  NOT NULL,
    created_at  DATETIME(3)  DEFAULT CURRENT_TIMESTAMP(3),

    INDEX idx_event_name (event_name),
    INDEX idx_user_id (user_id),
    INDEX idx_timestamp (timestamp)
);
```

### 6.3 前端 track() 函数骨架

```javascript
// utils/tracker.js
const SESSION_ID = crypto.randomUUID();

export function track(eventName, properties = {}) {
  const payload = {
    event_name: eventName,
    user_id: getCurrentUserId() || null,
    session_id: SESSION_ID,
    page_name: getCurrentPageName(),
    properties,
    timestamp: new Date().toISOString(),
  };

  // 用 sendBeacon 保证页面卸载时也能发出
  if (navigator.sendBeacon) {
    navigator.sendBeacon('/api/events', JSON.stringify(payload));
  } else {
    fetch('/api/events', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(payload),
      keepalive: true,
    });
  }
}
```

### 6.4 关键 SQL 查询示例（供 Metabase 配置）

**漏斗转化**：
```sql
SELECT
  COUNT(DISTINCT CASE WHEN event_name = 'page_view' AND page_name = 'home' THEN user_id END) AS step1_visit,
  COUNT(DISTINCT CASE WHEN event_name IN ('signup_complete','login_complete') THEN user_id END) AS step2_auth,
  COUNT(DISTINCT CASE WHEN event_name = 'subject_created' THEN user_id END) AS step3_subject,
  COUNT(DISTINCT CASE WHEN event_name = 'practice_started' THEN user_id END) AS step4_practice,
  COUNT(DISTINCT CASE WHEN event_name = 'practice_completed' THEN user_id END) AS step5_complete,
  COUNT(DISTINCT CASE WHEN event_name = 'retry_started' THEN user_id END) AS step6_retry
FROM events
WHERE timestamp >= DATE_SUB(NOW(), INTERVAL 7 DAY);
```

**D1/D3/D7 留存**：
```sql
WITH cohort AS (
  SELECT user_id, DATE(MIN(timestamp)) AS reg_date
  FROM events WHERE event_name = 'signup_complete'
  GROUP BY user_id
)
SELECT
  c.reg_date,
  COUNT(DISTINCT c.user_id) AS cohort_size,
  COUNT(DISTINCT CASE WHEN DATEDIFF(e.timestamp, c.reg_date) = 1 THEN c.user_id END) AS d1,
  COUNT(DISTINCT CASE WHEN DATEDIFF(e.timestamp, c.reg_date) = 3 THEN c.user_id END) AS d3,
  COUNT(DISTINCT CASE WHEN DATEDIFF(e.timestamp, c.reg_date) = 7 THEN c.user_id END) AS d7
FROM cohort c
LEFT JOIN events e ON c.user_id = e.user_id
  AND e.event_name IN ('practice_started','practice_completed','retry_started')
GROUP BY c.reg_date
ORDER BY c.reg_date;
```

---

## 七、看板页面结构（交给 Codex 搭建 Metabase 或自建页面）

建议分 **4 个 Tab**：

### Tab 1：全局概览
- 今日/本周 UV、新增注册、活跃用户 数字卡片
- 6 步转化漏斗图
- 最近 7 天趋势折线图（UV / 注册 / 完练人数）

### Tab 2：流失诊断
- 每步流失率柱状图 + 环比变化
- 上传失败原因分布（饼图）
- 答题中途放弃的题目序号分布（柱状图）
- AI 出题/批改耗时 P50/P95（折线图）
- 语音 ASR 编辑率趋势

### Tab 3：核心体验
- 客观题正确率分布（直方图）
- 主观题准确率分布（直方图）
- 错题重做进步率（重做前后准确率对比）
- 原文出处查看率
- 按科目类型对比各指标

### Tab 4：用户留存
- Cohort 留存热力图（D1/D3/D7 按注册日分组）
- 用户分层占比（新/激活/活跃/回流/流失）
- 单用户行为路径（可搜索具体 user_id 查看事件序列，用于内测期深度分析）

---

## 八、内测期优先级建议

内测初期（前 50 个用户）不需要一步到位，按优先级分批搭建：

**P0 — 第一周必须有**：
1. 6 步漏斗转化率（最核心，一眼看出哪里掉人）
2. AI 出题/批改耗时监控（体验底线）
3. 上传成功率和失败原因（如果上传都过不了，后面都白搭）

**P1 — 第二周补上**：
4. 完练率按题量分组分析
5. 语音 ASR 编辑率
6. D1/D3 留存

**P2 — 有精力再做**：
7. 题目质量反馈（需要在产品里加反馈入口）
8. 科目类型分群对比
9. 单用户行为路径查询

---

## 九、给 Codex 的执行摘要

1. 在现有数据库中新增一张 `events` 表（见第六节 SQL）
2. 前端封装 `track()` 工具函数，在 7 个页面的关键节点调用（见第五节埋点清单）
3. 后端新增 `POST /api/events` 接口，接收并写入事件
4. 部署 Metabase（Docker 一键部署），连接业务数据库
5. 按第七节的 4 个 Tab 配置看板，先做 P0 的 3 个视图
6. 关键 SQL 查询见第六节示例，可直接用于 Metabase 的 Custom Question
